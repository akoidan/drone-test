---
kind: pipeline
type: docker
name: default


platform:
  os: linux
  arch: arm64

node:
  runner: rpi

steps:
  - name: greeting
    commands:
      - echo hello world
trigger:
  event:
    exclude:
      - pull_request
